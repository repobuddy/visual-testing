import { Meta, Markdown ,Canvas} from "@storybook/addon-docs/blocks";

<Meta title="setup/createVis" />

# `createVis`

Import `createVis` from `vitest-plugin-vis/setup-api` (not from `vitest-plugin-vis/setup`).
