import { Meta, Markdown ,Canvas} from "@storybook/addon-docs/blocks";

<Meta title="setup/createVis" tags={['deprecated']} />

# `createVis`

🪦 Deprecated

`createVis` in `vitest-plugin-vis/setup` is deprecated.

It is moved to `vitest-plugin-vis/setup-api`.
